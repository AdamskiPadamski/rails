<%= form_for(@anime) do |f| %>
<% if @anime.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@anime.errors.count, "error") %> prohibited this anime from being saved:</h2>

	<ul>
		<% @anime.errors.full_messages.each do |message| %>
		<li><%= message %></li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="field">
	<%= f.label :title %><br>
	<%= f.text_field :title, class: 'form-control' %>
</div>
<div class="field">
	<%= f.label :year %><br>
	<%= f.number_field :year, class: 'form-control' %>
</div>
<div class="field">
	<%= f.label :number_of_episodes %><br>
	<%= f.number_field :number_of_episodes, class: 'form-control' %>
</div>
<div class="field">
	<%= f.label :description %><br>
	<%= f.text_area :description, class: 'form-control' %>
</div>
<div class="actions">
	<%= f.submit "Update anime", class: "btn btn-primary" %>
</div>
<% end %>
